// Describing the shape of the system's slice of state
export interface SetActiveZJComponentAction {
  id: string;
}

// Describing the different ACTION NAMES available
export const SET_ACTIVE_ZJ_COMPONENT: string = 'SET_ACTIVE_ZJ_COMPONENT';
